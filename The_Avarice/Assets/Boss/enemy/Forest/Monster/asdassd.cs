using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class asdassd : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        PlayerMgr.instance.setPlayerType(Player_Type.Paladin);
     
    }

   
}
